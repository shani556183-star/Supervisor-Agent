node_modules/
launcher.log

# Reports contain real client/prospect names, emails and business details
# pulled from the monitored agents. Keeping them out of git so nothing
# sensitive ends up on GitHub if this repo is public.
reports/*.html
